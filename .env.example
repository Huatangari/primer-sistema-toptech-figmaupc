# =============================================================================
# BuildTrack - Variables de entorno
# Copia este archivo a .env.local y completa los valores:
#   cp .env.example .env.local
#
# NUNCA subas .env.local al repositorio.
# =============================================================================

# Supabase - obten estos valores en:
# https://supabase.com/dashboard/project/<tu-proyecto>/settings/api
VITE_SUPABASE_URL=https://tu-proyecto.supabase.co
VITE_SUPABASE_ANON_KEY=tu-anon-key-publica

# Demo mode local: solo para presentaciones sin backend real.
# true  -> permite login demo y bypass de auth en rutas protegidas.
# false -> exige Supabase configurado (recomendado para QA y produccion).
VITE_ALLOW_DEMO_MODE=false

# Legacy API URL (reemplazado por Supabase client, mantener por compatibilidad)
VITE_API_URL=http://localhost:3000/api
